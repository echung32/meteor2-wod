import React from 'react';
import { Container } from 'react-bootstrap';

const Documentation = () => (
  <Container className="py-3">
    <h1>Documentation</h1>
    <p>First off, check the meteor-application-template-react github.io page.</p>
    <p>If that does not answer your question, you can post a smart question to the class Discord.</p>
    <p>If all else fails, post a question to the Meteor Forum site.</p>
  </Container>
);

export default Documentation;
